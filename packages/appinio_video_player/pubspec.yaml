name: appinio_video_player
description: A custom video player that builds on top of the official video_player package by flutter and adds many functionalities.
publish_to: 'none'

environment:
  sdk: ^3.8.1

dependencies:
  cached_video_player:
    git:
      url: https://github.com/NjiruClinton/flutter_cached_video_player
      ref: master
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
